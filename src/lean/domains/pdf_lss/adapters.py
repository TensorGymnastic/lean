"""PDF extraction backends for the pdf_lss domain.

Domain-specific extraction defaults are injected by
``lean.core.transports.yaml_loader._merge_extractor_defaults`` from
``CoreSettings``. The YAML manifest references the core extractors
directly — no adapter wrappers needed.
"""
